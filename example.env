# doge-threads — example environment (REQ0 §5 subset)
# Copy to `.env` locally. Do not commit real secrets.
# Values left empty; do not invent civic/cluster/schema-pack knobs.

APP_PROFILE=
DB_BACKEND=

SUPABASE_URL=
SUPABASE_SERVICE_ROLE=

LOG_LEVEL=
LOG_DEBUG_DIR=

SERVICE_API_TOKEN=
IDENTITY_BASE_URL=
GATEWAY_BASE_URL=

# Local default PORT=8001 (gateway uses 8000). Leave empty to use Makefile default.
PORT=
