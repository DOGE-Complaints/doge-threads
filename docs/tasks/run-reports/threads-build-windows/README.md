# Threads build windows

Производные артефакты CLI `--write-build-window` для профиля `threads`.

Не править вручную как SSOT очереди — источник: active `pkg-*.yaml`.

```bash
python3 docs/methodology/Zeya888-builder-queue/cli/builder_resolve_queue.py --project threads --write-build-window --window-flat-start 1 --window-flat-end K
```
